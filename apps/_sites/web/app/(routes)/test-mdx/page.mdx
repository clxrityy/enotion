# Welcome to MDX with GitHub Flavored Markdown

This is a **markdown** file with _JSX_ components and GitHub Flavored Markdown features!

## Task Lists

- [x] Set up MDX
- [x] Add GitHub Flavored Markdown support
- [ ] Add syntax highlighting
- [ ] Create documentation pages

## Tables

| Feature | Status | Notes |
|---------|--------|-------|
| Basic MDX | ✅ | Working |
| GFM | ✅ | Task lists, tables |
| Syntax Highlighting | 🚧 | Coming soon |

## Code Examples

Here's some inline `code` and a code block:

```javascript
const greeting = "Hello, MDX!"
console.log(greeting)

// GitHub Flavored Markdown supports syntax highlighting
function createComponent() {
  return <div>React component in MDX!</div>
}
```

```tsx
// TypeScript React component
interface Props {
  title: string;
}

export function MDXComponent({ title }: Props) {
  return (
    <div className="p-4 border rounded">
      <h2>{title}</h2>
    </div>
  );
}
```

## Strikethrough

~~This text is crossed out~~ but this text is normal.

## Links and References

Visit [GitHub](https://github.com) for more information.

## Blockquotes

> This is a blockquote in MDX
>
> It can span multiple lines and supports **formatting**.

## Emoji Support

You can use emoji: :rocket: :sparkles: :tada:

That's pretty cool, right? 🎉
